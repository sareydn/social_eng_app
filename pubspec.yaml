name: social_eng_app
description: A Flutter application for analyzing PDF and DOCX documents with OpenAI.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  file_picker: ^8.0.0+1
  http: ^1.1.0
  syncfusion_flutter_pdf: ^23.2.7
  docx_to_text: ^1.0.1
  path_provider: ^2.1.1
  speech_to_text: ^7.0.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/prompts/